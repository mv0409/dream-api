const Dream = require('../schema/dream-schema')

const getDreamsController = () => {
    return async(req) => {
        try {
            const dreams = await Dream.find
        } catch (error) {
            
        }
    }
}

